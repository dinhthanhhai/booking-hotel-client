export interface HeaderLink {
  name: string;
  url: string;
  note?: string;
}

export interface SidebarLink {
  name: string;
  url: string;
  icon: string;
}

export interface StayOption {
  id: number;
  name: string;
  note?: string;
}
